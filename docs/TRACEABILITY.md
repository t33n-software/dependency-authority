# Traceability

## Tickets

| Ticket | Change | Status |
|---|---|---|
| DA-1 | Establish the dependency authority core: candidate lifecycle domain model, admission policy evaluation, intake/admission/promotion/revalidation/revocation use cases, consumer-defined outbound ports, five lane controllers with fail-closed bootstrap wiring, source-quality gates, CodeQL, dependency admission review, Dependabot, Lefthook, and importable Rulesets. | In progress |
| DA-2 | Migrate the module path and Go import paths to the `t33n-software` organization namespace; add the LF line-ending contract (`.gitattributes`) and the push-protections Ruleset source `00-push-protections.json` in the verified GitHub export format. | In progress |
| DA-3 | Align the Go 1.26.6 toolchain and source gates with the supply chain fortress contract: pinned `tools/` module with govulncheck, staticcheck, and Lefthook; fail-closed vulnerability analysis; fuzz smoke lane for the inbound configuration boundary; Lefthook configuration validation and commit-msg hook; daily CI re-scan. | In progress |
| DA-4 | Bind the Go lane design (ADR-0002): adapter placement in `internal/dependency/adapters/outbound/` with a provider-free domain and application core; OSV as the pinned reference scanner engine with snapshot-based scanning and recorded tool identity; `dependency-policy/v1` consumption through exact schema version pins with fail-closed admission; the automatic policy-pass approval with time-bounded instance-overlay exceptions; the seven protected `dep-*` lane environments matching the organization WIF federation; the consumer fetch and isolated build contract; and the empirical perimeter intake check at first lane execution. | In progress |
| DA-6 | Implement the Go lane outbound adapters and lane workflows: the upstream Go proxy digest client with GOPROXY escaping and no public fallback; the pinned `dependency-policy/v1` bundle loader; the offline OSV scanner adapter with CVSS v3 base scoring and a conservative severity fallback; the append-only candidate records store, the approved-zone publisher with the dirhash content-identity proof, and the package-scoped download-rule revocation gate on Artifact Registry; the append-only evidence reference index; the validated lane environment bindings with a fuzzed loader; the environment-driven controller wiring with fail-closed partial bindings; and the seven dispatch-only `dep-*` lane workflows with environment-scoped WIF federation and the intake boundary probe. | In progress |
| DA-7 | Execute the lane use cases: the validated operation inputs (module, version, lane identity, scanner and scanner-database identities, approval TTL, revocation reason) bound from the environment with a fuzzed loader; the evidence payload publication with content-addressed, digest-bound references next to the append-only index; the content-pinned policy identity; the controller execution contract (intake registers the pending candidate; admission scans, records scan and decision evidence, and records the automatic time-bounded approval on a policy pass; promotion promotes under the newest recorded, still valid approval; revalidation re-evaluates and records the fresh evidence; revocation blocks downloads and records the revocation evidence); the conventional `--version` surface of the five lane controllers; the schema-v3 quality configuration; and the dispatch inputs wired into the five lane workflows. | In progress |
| DA-8 | Adopt the canonical repo surface (the pilot): the byte-identical canonical callers (`ci.yml`, `codeql.yml`, `dependency-review.yml`) pinned to the repository-governance home by full-length commit SHA; the canonical file family (`.gitignore`, `lefthook.yml`) and the materialized `.github/CODEOWNERS`; the `repo-bindings.json` tenant manifest; the thin `canonical-conformance.yml` workflow running the home verifier; the tooling-module pins for `quality-gate` and `check-coverage` (go-quality-authority v1.0.1) and `verify-canonical` (repository-governance v1.0.0); the conventional `--version` surface of the development tools; and the packaging contract bound to the manifest. | In progress |
| DA-10 | Reference the canonical gate chain through the tooling module pin: the repo-local chain copies `cmd/build` and `cmd/check-coverage` are removed, the quality configuration invokes the go-quality-authority orchestrator via `go tool -modfile tools/go.mod quality-gate`, the restated `defaults` block is dropped for the schema-owned `includeFamilies` default (SCG-9), and the contract guards prove the canonical invocation, the absent `defaults` block, and the absence of both copies fail-closed. | In progress |
| DA-11 | Onboard the license-hub render-and-verify lane: the tenant values `license.values.json` and the digest-pinned lock `license.lock.json` (template `license-hub/templates/custom/norepublish/NoRepublish-1.0.0.hbs`, version 1.0.0), the rendered `LICENSE` and `LICENSES/LicenseRef-dependency-authority-NoRepublish-1.0.txt` instance proven byte-identical against the canonical render, and the binding manifest flip `licenseHub: true` that activates the canonical conformance wiring proof. | In progress |
| DA-14 | Adopt the release-lifecycle caller family: the seven byte-identical canonical callers (`release-control.yml`, `release-reconciliation.yml`, `execute-protected-line-request.yml`, `tag-promoted-release.yml`, `publish-release-artifacts.yml`, `hotfix-delivery.yml`, `hotfix-propagation.yml`) pinned to the git-governance home by full-length commit SHA; the `git-governance` CLI tooling-module pin that lets the tenant lanes build the pinned class-D tool from `tools/go.mod`; and the packaging contract guards that prove the caller byte-identity and the family pin fail-closed. | In progress |
| DA-16 | Add the controller image substrate (GO-SCF-019): the single parameterized root `Dockerfile` packaging a locally built lane controller binary (`ARG CONTROLLER` over the five lane controllers) on the digest-pinned minimal non-root runtime (`gcr.io/distroless/static-debian12:nonroot@sha256:afa5c872…`, the index digest resolved and proven at the upstream), with an explicit numeric non-root user and no floating syntax frontend reference; the build and delivery runbook `docs/operations/controller-image-substrate.md` binding the controlled-toolchain build flags, the staging-only delivery with the read-back digest proof, and the promotion-only release fill; and the packaging contract guards for the digest pin, the controller enumeration, and the register entry. | In progress |
| DA-17 | Slim the seven `dep-*` lane workflows to the trigger form: each lane authenticates as the dedicated invoke-only trigger identity of its operation (`DEP_<OPERATION>_TRIGGER_SERVICE_ACCOUNT`), invokes the zone-resident workload job over the compute control plane with the operation inputs as execution parameters (`gcloud run jobs execute --wait` with `--update-env-vars`; the admission lane keeps the approval TTL `72h` as the versioned workflow value, the revocation lane passes the reason input), proves the execution status fail-closed over the same plane (`gcloud run jobs executions describe`), and reports the control-plane audit pointer; the in-lane controller build, the Go toolchain steps, the raw access-token handling, the data-plane variables, and the intake boundary probe are removed (the job answers the perimeter question from inside); ADR-0002 records the trigger-form decision; and the packaging contract guards bind the trigger identity variables, the job coordinates, the invoke/read-back form, and the removal of the retired in-lane execution form fail-closed. | In progress |
| DA-18 | Bind the in-perimeter ADC token source: the lane controllers obtain short-lived access tokens from the identity attached to the workload through the provider instance metadata mechanism — a dependency-free `net/http` metadata client with the minimal scope, process-memory-only caching until the expiry minus a safety skew, single-flight refresh, and fail-closed behavior outside the provider runtime; the static `DEPENDENCY_AUTHORITY_ACCESS_TOKEN` environment form is removed without a fallback; the `--version` surface stays auth-free. | In progress |
| DA-19 | Correct the lane execution status read-back to the status-wrapped form: the live read of the intake execution proved that the v2 execution resource carries the status fields under the `status` wrapper, so all seven `dep-*` lane workflows now assert `.status.completionTime`, `.status.succeededCount` and `.status.failedCount` and report `.status.completionTime` and `.status.logUri` (the top-level `.name` reference stays); the packaging contract guard binds the corrected assertion and report form fail-closed across all seven lanes and gains a regression guard that forbids the retired top-level read paths. | In progress |
| DA-20 | Materialize the workload tooling channel at controller start: the domain identity grammar for the pinned scanner tool (`osv-scanner/<version>/<asset>@sha256:<digest>`), scanner database snapshot (`osv-db/<ecosystem>@sha256:<digest>`), and admission policy bundle (`dependency-policy/v1@sha256:<digest>`) with fail-closed parsing and a boundary fuzz lane; the read-only channel materializer that resolves every object through the evidence-zone repository inventory, downloads it over the authenticated Artifact Registry transport, proves the content against the identity digest fail-closed, and places it atomically and idempotently (no partial placement, no overwrite of differing content); the `DEPENDENCY_AUTHORITY_POLICY_BUNDLE_IDENTITY` binding; the bootstrap materialization step (the admission and revalidation controllers materialize tool, database, and bundle, the promotion controller materializes the bundle) with its fail-closed runtime proof; and the scanner database snapshot layout helper. | In progress |
| DA-21 | Materialize the candidate content and bind the evidence to the proven channel identities: the candidate content materialization in the artifactregistry adapter fetches the module archive from the controlled intake boundary, proves it against the recorded candidate digest fail-closed, and places it at the canonical content path (`<root>/<ecosystem>/<name>@<version>`, now owned by the candidate domain model and consumed by the scanner and the materializer) through an atomic, marker-anchored, idempotent placement that never leaves a partial target; the admission and revalidation lanes materialize the candidate through the new candidate content port before any scan; and the scan evidence carries only the channel identities proven against the materialized artifacts (the tool and database identities re-proven at evidence time, the policy identity derived from the bundle content), never the asserted operation inputs. | In progress |
| DA-22 | Bind the scanner channel identities on the scanning lanes: the admission and revalidation lane workflows carry the pinned scanner tool identity (`osv-scanner/<version>/<asset>@sha256:<digest>`) and the pinned scanner database snapshot identity (`osv-db/<ecosystem>@sha256:<digest>`) as versioned workflow values passed as execution parameters (a pin bump is a governed pull request), satisfying the fail-closed operation input contract of the scanning operations from the lane surface; the packaging contract guards bind the exact pinned identity values fail-closed, and the lane environment sweep is refined with the documented exception for the two identity operation inputs while the retired data-plane scanner path forms stay forbidden on every lane. | In progress |
| DA-23 | Migrate the approved-zone publisher to the direct Artifact Registry upload: the publisher uploads the digest-proven module archive bytes through the `goModules:create` multipart media upload and waits fail-closed for the upload operation (a bounded poll with a context-aware wait seam), replacing the `gcloud artifacts go upload` child process that the pure controller image cannot carry; the content-identity proof (the intake digest proof plus the pre/post-publication dirhash comparison) is unchanged; the child-process runner seam is removed and the module workspace factory moves to `workspace.go`. | In progress |
| DA-24 | Correct the tooling channel inventory resolution to the platform resource-name form: the Artifact Registry file inventory carries the file path URL-encoded in the resource name (the path slashes as `%2F`), so the channel materializer compares the canonical decoded form against the bound object coordinate and keeps the server-issued encoded name for the download, replacing the raw string equality that failed closed on every real inventory lookup; the whitebox fixtures carry the platform-proven encoded name form, and a regression guard binds the encoded resolution and the encoded download path fail-closed. | In progress |

## Scope boundaries

- DA-1 delivers the source-level core only. It does not deliver trust-zone
  adapter implementations, a versioned release, an artifact delivery lane,
  or any organization- or tenant-bound content.
- The outbound adapters for upstream, scanner, policy, evidence, registry,
  and download-gate ports land in `internal/dependency/adapters/outbound/`
  with the trust-zone lane enablement (ADR-0002); the domain and application
  core stays provider-free.
- The `release/*` and `support/*` branch families and their Rulesets are
  activated only with a complete governed release lifecycle.
